const Button = (props) => (
  <button className="su-button su-bg-digital-red su-text-white hover:su-bg-black focus:su-bg-black su-font-regular su-leading-none">
    {props.label}
  </button>
);

export default Button;