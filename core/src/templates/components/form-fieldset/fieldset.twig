{#
/**
 * @file
 * Form fieldset container element with a legend
 *
 * A form fieldset component to group fields
 *
 * Available variables:
 * - attributes: For additional HTML attributes not already provided.
 * - fieldset_attributes: For adding additional HTML attributes to the input field.
 * - modifier_class: Additional CSS classes to change look and behaviour of the component.
 * - fieldset_legend_modifier_class: Additional CSS classes to change look and behaviour of the fieldset.
 * - fieldset_legend: heading for the fieldset
 * - fieldset_body: placeholder for the body of the fieldset
 *
 */
#}

<fieldset {{ attributes }} class="su-fieldset {{ modifier_class }}">
  <legend {{ attributes }} class="su-fieldset--legend {{ modifier_class }}">{{ fieldset_legend }}</legend>
  {{ fieldset_body }}
</fieldset>


