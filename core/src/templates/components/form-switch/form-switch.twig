{#
/**
 * @file
 * Input checkbox element as a switch
 *
 * A switch looking checkbox
 *
 * Available variables:
 * - attributes: For additional HTML attributes not already provided.
 * - switch_checkbox_attributes: For adding additional HTML attributes to the input field.
 * - modifier_class: Additional CSS classes to change look and behaviour of the component.
 * - switch_text_before: Text label for the field before the switch ui.
 * - switch_text_after: Text label for the field after the switch ui.
 *
 */
#}
<label {{ attributes }} class="su-switch {{ modifier_class }}">
  <input type="checkbox" {{ switch_checkbox_attributes }} />
  {% if switch_text_before is not empty %}
    <span class="su-switch__label-before">{{ switch_text_before }}</span>
  {% endif %}
  <span class="su-switch__bar">
    <span class="su-switch__slider"></span>
  </span>
  {% if switch_text_after is not empty %}
    <span class="su-switch__label-after">{{ switch_text_after }}</span>
  {% endif %}
</label>
