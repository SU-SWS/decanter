
services:
  # What to call the service hosting the site. Because there is only
  # one service, it is automatically set as the default service, which
  # does a few things
  #   1. Clones the git repository into the service container
  #   2. Exposes port 80 to the Tugboat HTTP proxy
  #   3. Routes requests to the preview URL to this service
  apache:

    # Use the available version of Apache by not specifying a version
    image: pookmish/tugboat-nvm:latest

    # A set of commands to run while building this service
    commands:

      # Commands that set up the basic preview infrastructure
      init:
        - echo "Welcome Back Boss"
        - /bin/bash source ~/.nvm/nvm.sh
        - /bin/bash nvm install 10.15.1

      build:
        - /bin/bash nvm use
        - /bin/bash npm install
        - /bin/bash npm run build
        - /bin/bash npm run styleguide
        - /bin/bash rm -R node_modules
        - /bin/bash ln -snf "${TUGBOAT_ROOT}/styleguide" "${DOCROOT}"

    # Collection of urls to compare visual results.
    # visualdiffs:
    #   - /
    #   - /news
    #   - /events
    #   - /people
    #   - /research
    #   - /about
